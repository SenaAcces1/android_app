package com.adso.senaaccess.ui.theme // REEMPLAZA por el nombre real de tu paquete

import androidx.compose.ui.graphics.Color

/**
 * PALETA DE COLORES - ESTILO VERDE PROFESIONAL
 * Ideal para proyectos de desarrollo de software (SenaAccess)
 */

// --- Tonos Verdes Principales (Identidad Visual) ---
val GreenSena = Color(0xFF2E7D32)      // Verde base (Primario)
val GreenDark = Color(0xFF1B5E20)      // Verde oscuro para botones presionados
val GreenLight = Color(0xFFE8F5E9)     // Verde suave para fondos o "hover"
val GreenAccent = Color(0xFF4CAF50)
val SoftGray = Color(0xFFF8F9FA)

// Verde brillante para acentos o estados activos

val NeonGreen = Color(0xFF00FF00)      // Verde Neón vibrante
val DarkBg = Color(0xFF121212)         // Fondo casi negro
val SurfaceDark = Color(0xFF1E1E1E)    // Fondo de la tarjeta central
val TextGray = Color(0xFF808080)      // Texto secundario/placeholders

// --- Colores de Fondo y Superficie (Neutros) ---
val BackgroundGray = Color(0xFFF8F9FA) // Fondo general de la app (limpio)
val SurfaceWhite = Color(0xFFFFFFFF)   // Color de los campos de texto y tarjetas
val SoftOutline = Color(0xFFE0E0E0)    // Borde gris suave para inputs inactivos

// --- Colores de Texto ---
val TextPrimary = Color(0xFF1C1B1F)    // Texto negro principal
val TextSecondary = Color(0xFF49454F)  // Texto gris para descripciones
val TextOnGreen = Color(0xFFFFFFFF)    // Texto blanco para usar sobre fondo verde

// --- Colores para Modo Oscuro (Dark Theme) ---
val DarkBackground = Color(0xFF121212) // Fondo negro mate
val DarkSurface = Color(0xFF1E1E1E)    // Tarjetas en modo oscuro
val GreenSenaDark = Color(0xFF81C784) // Verde pastel para que resalte en negro

// --- Estados Adicionales ---
val ErrorRed = Color(0xFFB00020)       // Rojo para validaciones fallidas
val SuccessGreen = Color(0xFF388E3C)   // Verde de éxito para registros